<Window x:Class="KSVA2._0_WPF.Views.DateSelectionDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KSVA2._0_WPF.Views"
        mc:Ignorable="d"
        Title="Select Session Date" Height="150" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DatePicker x:Name="DatePickerControl" Margin="0 0 0 10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button x:Name="Okbtn" Content="OK" Width="75" Margin="100, 30" Click="OkButton_Click"/>
            <Button x:Name="Cancelbtn" Content="Cancel" Width="75" Margin="5, 30" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
